import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs/blocks';
import DModal from '../components/d-modal.vue';
import DButton from '../components/d-button.vue';
import { COLOR_VARIANTS, SIZES } from '../utils';

<Meta 
  title="Elements/Modal" 
  component={DModal} 
  argTypes={{
    variant: { control: { type: 'select', options: COLOR_VARIANTS } },
    accent: { control: { type: 'select', options: COLOR_VARIANTS } },
    width: { control: { type: 'select', options: SIZES } },
    closeOptions: { control: { type: 'select', options: ['header', 'footer', 'both']}}
  }}
/>

# Modal
<ArgsTable story="Modal" />

# DModal component
```html
<DButton name="Show Modal" @click="showModal = true" />
<DModal v-if="showModal" title="Modal Title" @close="showModal = false" />
```

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { DModal, DButton },
  data() {
    return {
      show: true
    }
  },
  template: `
        <div>
          <DButton name="Show Modal" @click="show = true" variant="link"/>
          <DModal v-if="show" :variant="variant" :accent="accent" :width="width" :closeOptions="closeOptions" @close="show = !show">
            <template #content>
              <div class="content">
                <h1>Lorem ipsum</h1>
                <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.</p>
              </div>
            </template>
          </DModal> 
        </div>
        `
});

<Canvas>
  <Story name="Modal" args={{variant: 'light', accent: 'warning', width: 'small', closeOptions: 'both' }}>
    {Template.bind({})}
  </Story>
</Canvas>

