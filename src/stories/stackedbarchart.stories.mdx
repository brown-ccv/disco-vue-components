import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs/blocks';
import DChartContainer from '../components/d-chart-container.vue';
import DStackedBarChart from '../components/d-chart-stacked-bar.vue';

<Meta
    title="Visualization/Charts/Stacked Bar Chart"
    component={DStackedBarChart}
/>


# Stacked Bar Chart

A Stacked Bar Chart shows aggregated data over categorical values on the x axis.

<ArgsTable story="Stacked Bar Chart" />

## DStackedBarChart component
#this is just a markdown code chunk.
```html
<DStackedBarChart
  id="stackedbarchart"
  :dataset="[
    { month: 1, group: 'A', amount:25},
    { month: 1, group: 'B', amount:25},
    { month: 1, group: 'C', amount:25},
    { month: 2, group: 'A', amount:25},
    { month: 2, group: 'B', amount:25},
    { month: 2, group: 'C', amount:25},
    { month: 3, group: 'A', amount:25},
    { month: 3, group: 'B', amount:25},
    { month: 3, group: 'C', amount:25}
  ]"
  :min-width="400"
  x="month"
  x-label="Month"
  y="group"
  y-label="Group"
  :include-actions="true"
/>
```

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { DChartContainer, DStackedBarChart },
  template: `
        <DChartContainer width="full">
          <template #title>Stacked Bar Chart</template>
          <template #description>
            A stacked bar chart shows the amount for different categories in a single bar.
          </template
          >
          <template #chart>
            <DStackedBarChart
              id="stackedbarchart"
              :dataset="dataset"
              :min-width="minWidth"
              :height="height"
              :x="x"
              :x-label="xLabel"
              :y="y"
              :y-label="yLabel"
              :include-actions="includeActions"
              :enable-darkmode="enableDarkmode"
            />
          </template>
        </DChartContainer>
    `
});

<Canvas>
  <Story name="Stacked Bar Chart" args={{
    includeActions: true,
    minWidth: 400,
    height: 300,
    x: 'category',
    y: 'amount',
    xLabel: 'Letters',
    yLabel: 'Amount',
    dataset: [
      { category: 'A', amount: 28},
      { category: 'B', amount: 55},
      { category: 'C', amount: 43}
    ],
    enableDarkmode: true,
  }}>
    {Template.bind({})}
  </Story>
</Canvas>