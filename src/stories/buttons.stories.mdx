import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs/blocks';
import DButton from '../components/d-button.vue';
import DIcon from '../components/d-icon.vue';
import { action } from "@storybook/addon-actions";

<Meta 
    title="Elements/Buttons" 
    component={DButton}
    argTypes={{
        variant: {control: {type: 'select', options: [
            'primary',
            'success',
            'danger',
            'warning',
            'info',
            'link',
            'dark',
            'light'
          ]}},
        size: {control: {type: 'select', options: ['small', 'medium', 'large']}}
    }}
/>

# Buttons
<ArgsTable story="Buttons"/>

# DButton component
```jsx
<DButton name="button"/>
```

```jsx
<DButton name="button">
    <template v-slot:icon-right>
        <span class="icon">
            <DIcon name="carrot-right" />
        <span>
    </template>
</DButton>
```

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  methods: { action: action("clicked") },
  components: { DButton, DIcon },
  template: `<DButton @click="action" :name="name" :variant="variant" :outlined="outlined" :size="size">
                <template v-slot:icon-right>
                    <span class="icon">
                        <DIcon name="carrot-right" />
                    <span>
                </template>
            </DButton>`
});

<Canvas>
  <Story name="Buttons" args={{name: 'button', variant: 'warning', outlined: false, size: 'small'}} >
    {Template.bind({})}
  </Story>
</Canvas>