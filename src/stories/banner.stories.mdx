import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs/blocks';
import DBanner from '../components/d-banner.vue';
import DIcon from '../components/d-icon.vue';
import { COLOR_VARIANTS } from '../utils';

<Meta 
    title='Elements/Banner' 
    component={DBanner} 
    argTypes={{
        variant: { control: { type: 'select', options: COLOR_VARIANTS } },
        accent: { control: { type: 'select', options: COLOR_VARIANTS } },
        tagSize: { control: { type: 'select', options: ['normal', 'medium', 'large'] } }
    }}
/>

# Banner

<ArgsTable story='Banner' />

### DBanner component
```html
<DBanner text="Powering CCV\'s Web Apps" />
```

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { DBanner },
  template: `<DBanner :text='text' :variant='variant' :accent='accent' :tagSize='tagSize'/>`
});

<Canvas >
    <Story name='Banner' args={{variant: 'primary', text: 'Powering CCV\'s Web Apps', accent: 'danger', tagSize: 'normal'}}>
        {Template.bind({})}
    </Story>
</Canvas>

export const TemplateIcon = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { DIcon, DBanner },
  template: `<a href="#"><DBanner :text='text' :variant='variant' :accent='accent' :tagSize='tagSize'>
              <template #badge>
                <span>&#9432; Disco Design System</span>
              </template>
            </DBanner></a>`
});

To add a badge to the left of the text, use the slot `#badge`. This example wraps the banner in a `a` tag to demonstrate how to create a banner with link.

<Canvas >
    <Story name='Banner Icon' args={{variant: 'primary', text: 'Powering CCV\'s Web Apps', accent: 'danger', tagSize: 'normal'}}>
        {TemplateIcon.bind({})}
    </Story>
</Canvas>