import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs/blocks';
import DBanner from '../components/d-banner.vue';
import DIcon from '../components/d-icon.vue';
import { COLOR_VARIANTS } from '../utils';

<Meta 
    title='Elements/Banner' 
    component={DBanner} 
    argTypes={{
        variant: { control: { type: 'select', options: COLOR_VARIANTS } },
        tagVariant: { control: { type: 'select', options: COLOR_VARIANTS } }
    }}
/>

# Banner

<ArgsTable story='Banner' />

### DBanner component
```html
<DBanner text="Powering CCV\'s Web Apps" />
```

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { DBanner },
  template: `<DBanner :text='text' :tag='tag' :variant='variant' :tagVariant='tagVariant'/>`
});

<Canvas >
    <Story name='Banner' args={{variant: 'primary', tag: 'Disco Design System', text: 'Powering CCV\'s Web Apps', tagVariant: 'danger'}}>
        {Template.bind({})}
    </Story>
</Canvas>

export const TemplateIcon = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { DIcon, DBanner },
  template: `<DBanner :text='text' :tag='tag' :variant='variant' :tagVariant='tagVariant'>
              <template slot="icon">
                <DIcon name="info" width=5 height=5/>
              </template>
            </DBanner>`
});

<Canvas >
    <Story name='Banner Icon' args={{variant: 'primary', tag: 'Disco Design System', text: 'Powering CCV\'s Web Apps', tagVariant: 'danger'}}>
        {TemplateIcon.bind({})}
    </Story>
</Canvas>