import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs/blocks';
import DBanner from '../components/d-banner.vue';
import DIcon from '../components/d-icon.vue';
import { COLOR_VARIANTS } from '../utils';

<Meta 
    title='Elements/Banner' 
    component={DBanner} 
    argTypes={{
        variant: { control: { type: 'select', options: COLOR_VARIANTS } },
        accent: { control: { type: 'select', options: COLOR_VARIANTS } },
        tagSize: { control: { type: 'select', options: ['normal', 'medium', 'large'] } }
    }}
/>

# Banner

<ArgsTable story='Banner' />

### DBanner component
```html
<DBanner text="Powering CCV\'s Web Apps" />
```

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { DBanner },
  template: `<DBanner :text='text' :tag='tag' :variant='variant' :accent='accent' :tagSize='tagSize'/>`
});

<Canvas >
    <Story name='Banner' args={{variant: 'primary', tag: 'Disco Design System', text: 'Powering CCV\'s Web Apps', accent: 'danger', tagSize: 'normal'}}>
        {Template.bind({})}
    </Story>
</Canvas>

export const TemplateIcon = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { DIcon, DBanner },
  template: `<DBanner :text='text' :tag='tag' :variant='variant' :accent='accent' :tagSize='tagSize'>
              <template slot="icon">
                <DIcon name="info"/>
              </template>
            </DBanner>`
});

<Canvas >
    <Story name='Banner Icon' args={{variant: 'primary', tag: 'Disco Design System', text: 'Powering CCV\'s Web Apps', accent: 'danger', tagSize: 'normal'}}>
        {TemplateIcon.bind({})}
    </Story>
</Canvas>